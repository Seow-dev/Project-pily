export interface DataTypes {
  megazineId?: number; 
  title?: string;
  subTitle?: string;
  author?: string;
  authorImg?: string;
  createdAt?: string;
  thumbnail?: string;
}

export interface UserData {
  profileImage?: string // url,
  username: string     // username              
}